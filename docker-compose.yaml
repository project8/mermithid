version: "3"

services:
  morpho:
    build: .
    command: >
      bash -c "source /setup.sh && bash"
    volumes:
      # share a subdirectory from the host to /host in the docker (can be edited)
      - ~/mermithid:/host
